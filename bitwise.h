#ifndef _BITWISE_H_
#define _BITWISE_H_

int hamming_weight(int);
int hamming_distance(const char *, const char *, unsigned int);
int single_hamming_distance(const char, const char);
double norm_hamming_distance(char *, long , long);

#endif
